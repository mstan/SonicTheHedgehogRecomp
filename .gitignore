build/
build-rdb/
*.bin
*.bmp
framedump/
screenshots/
*.bin
*.bin
build/
last_rom.txt
screenshots/
*.log
rdb_*.json
**/__pycache__/

# segagenesisrecomp is a directory junction to the top-level canonical checkout
# (../segagenesisrecomp). Not tracked here — it lives in its own repo.
segagenesisrecomp
build-*/

# release zips are published as GitHub release assets, never committed
release/*.zip
